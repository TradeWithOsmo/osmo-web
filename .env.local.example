# Local overrides — copy to .env.local
# Values here override .env for local dev only

# Backend API URL (local or VPS)
VITE_API_URL=http://localhost:8000

# Frontend port
VITE_PORT=3000

# Enable verbose API logging for debugging
VITE_DEBUG_API=false
